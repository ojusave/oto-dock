"""Execution layer implementations.

Each subdirectory is a self-contained layer package:
  cli/       -- Claude Code CLI subprocess layer
  direct/    -- Direct LLM API layer (multi-provider)
  providers/ -- LLM provider adapters (used by direct layer)
  codex/     -- OpenAI Codex CLI layer (future)
"""
